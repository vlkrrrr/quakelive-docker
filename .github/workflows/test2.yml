name: second
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
        - name: get code
          uses: actions/checkout@v3
        - name: install nodejs
          uses: actions/setup-node@v3
          with:
            node-versions: 18
        - name: Ouput stuff
          run: echo "${{toJson(github)}}"
